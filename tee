<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IE Boiler Pathways</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 20px;
        }
        .container {
            background: white;
            padding: 20px;
            border-radius: 8px;
            max-width: 800px;
            margin: auto;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            color: #6d542d;
            text-align: center;
        }
        label {
            font-weight: bold;
            display: block;
            margin-top: 10px;
        }
        select, input {
            width: 100%;
            padding: 8px;
            margin-top: 5px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }
        .search-btn {
            background-color: #6d542d;
            color: white;
            padding: 10px;
            border: none;
            width: 100%;
            margin-top: 15px;
            font-size: 16px;
            cursor: pointer;
            border-radius: 4px;
        }
        .search-btn:hover {
            background-color: #5a4523;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>IE Boiler Pathways Search Tool</h1>
        <form style="column-count: 3;column-width: 40;">
            <label for="country">Country</label>
            <select id="country">
                <option>Any Country</option>
                <option>USA</option>
                <option>Spain</option>
                <option>Brazil</option>
            </select>
            
            <label for="region">Region</label>
            <select id="region">
                <option>Any Region</option>
            </select>
            
            <label for="year">Graduation Year</label>
            <input type="text" id="year" placeholder="Enter Year">
            
            <label for="industry">Industry & Sector</label>
            <select id="industry">
                <option>Any Industry</option>
            </select>
            
            <label for="gender">Gender</label>
            <select id="gender">
                <option>Select</option>
                <option>Male</option>
                <option>Female</option>
                <option>Non-binary</option>
            </select>
            
            <label for="ethnicity">Ethnicity</label>
            <select id="ethnicity">
                <option>Select all that apply</option>
            </select>
            
            <button type="button" class="search-btn">Search</button>
        </form>
    </div>
</body>
</html>
